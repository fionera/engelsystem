<ul class="nav navbar-nav navbar-right">
    <li class=""><a href="https://engel.ctdo.de/register"><span class="glyphicon glyphicon-plus"></span>
            Register</a></li>
    <li class="active"><a href="https://engel.ctdo.de/login"><span class="glyphicon glyphicon-log-in"></span>
            Login</a>
    </li>

    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu" style="max-height: 822px; overflow-y: scroll;">
            {% for locale in locales %}
                <li class=""><a href="{{ url('logout') }}">{{ locale|trans }}</a></li>
            {% endfor %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class=""><a href="{{ url('logout') }}">{% trans %}Logout{% endtrans %}</a></li>
            {% endif %}
        </ul>
    </li>
</ul>