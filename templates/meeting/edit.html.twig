{% extends 'base.html.twig' %}

{% block title %}Meeting{% endblock %}

{% block content %}
    <div class="col-md-12">
        <h1>Edit Meetings:</h1>
        {{ form_start(meetingForm) }}
        {{ form_widget(meetingForm) }}
        {{ form_end(meetingForm) }}

        {% if hasPermission('meeting_remove_all') or (hasPermission('meeting_remove') and comment.author == user.username) %}
        <a class="btn btn-danger" href="{{ url('meeting_delete', {'id': meeting.id}) }}">
            <span class="glyphicon glyphicon-trash"></span> Delete
        </a>
        {% endif %}
    </div>

{% endblock %}
